# vim
To use this add at least the following in ~/.vimrc:

    execute pathogen#infect()
    syntax on
    filetype on
    filetype indent on
    filetype plugin on
    set tabstop=2
    set expandtab
    set shiftwidth=2
  
  
